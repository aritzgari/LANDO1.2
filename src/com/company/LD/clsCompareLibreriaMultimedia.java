package com.company.LD;

import java.util.Comparator;

/*public class clsCompareLibreriaMultimedia implements Comparator<clsCompareLibreriaMultimedia> {
    public int compare(clsLibreria_MultimediaBD A1 , clsLibreria_MultimediaBD A2) {// las variables creadas de objLibreriaMultimedia
        if (A1.getNombre().compareTo(A2.getNombre()) == 0) { // en caso de ser nombres iguales coger la descripcion
            return A1.getDescripcion().compareTo(A2.getDescripcion());
        } else {
            return A1.getNombre().compareTo(A2.getNombre());
        }
    }

}*/
// Esto solo tiene unos ligeros cambios respecto a uno de los programas que he encontrado es de unai y esta en el foro
// tambien tienen comparaciones de variables de otros tipos.